# fail2ban filter for ForgeBase panel login failures.
# pgforged logs "FAILED LOGIN ip=<ip> user=..." to its journal on every failed
# panel login; the jail watches the pgforged systemd unit for these lines.
[Definition]
failregex = FAILED LOGIN ip=<HOST>
ignoreregex =
